---

configurator:

  directories:
    - dir: /backup
    - dir: /data

  fulldirs:
    configs:
      src: files/configs
      dest: /data
    docker-stack:
      src: files/docker-stack
      dest: /data

  files:
    # rsnapshot
    - file: /etc/rsnapshot.conf
      src: files/configs/rsnapshot/rsnapshot.conf
    - file: /etc/cron.d/rsnapshot
      src: files/configs/rsnapshot/rsnapshot.cron.d
